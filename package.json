{
  "name": "techsurf-chat-platform",
  "version": "1.0.0",
  "description": "Universal AI-powered chat widget platform with Contentstack integration",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "sdk"
  ],
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:sdk\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm start",
    "dev:sdk": "cd sdk && npm run dev",
    "build": "npm run build:production",
    "build:production": "bash scripts/build-all.sh",
    "start": "cd server && npm start",
    "deploy": "bash scripts/deploy.sh",
    "postinstall": "npm run install:all",
    "install:all": "cd client && npm ci --legacy-peer-deps && cd ../server && npm ci --legacy-peer-deps && cd ../sdk && npm ci --legacy-peer-deps"
  },
  "keywords": [
    "chat",
    "widget",
    "ai",
    "contentstack",
    "customer-support",
    "techsurf"
  ],
  "author": "TechSurf Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^8.2.0",
    "rollup": "^4.52.0",
    "typescript": "^5.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
